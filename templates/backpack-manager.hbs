<form class="backpack-manager">
  <section class="stowed">
    <header class="stowed-header" title="{{localize 'BACKPACK_MANAGER.STOW_HEADER'}}">
      <div class="header-name">{{localize 'BACKPACK_MANAGER.STOWED'}}: {{bag}}</div>
      <div class="header-encumbrance">({{bagValue}} / {{bagMax}})</div>
    </header>
    <div class="stowed-content">
      {{#each stowed as |item|}}
      <div class="item">
        <div class="item-details" data-uuid="{{item.item.uuid}}">
          <img src="{{item.item.img}}">
          <a title="{{localize 'BACKPACK_MANAGER.SHOW'}}">
            {{item.item.name}} {{#if (backpackManagerCheckqty item.quantity)}}({{item.quantity}}){{/if}}
          </a>
        </div>
        <div class="functions">
          {{#if (backpackManagerCheckqty item.quantity)}}
          <div class="quantities">
            <a data-type="less" title="{{localize 'BACKPACK_MANAGER.LESS'}}">
              <i class="fa-solid fa-arrow-left"></i>
            </a>
            <input type="number" title="{{localize 'BACKPACK_MANAGER.VALUE'}}" class="current" value="{{item.quantity}}"
              data-max="{{item.quantity}}" disabled>
            <a data-type="more" title="{{localize 'BACKPACK_MANAGER.MORE'}}">
              <i class="fa-solid fa-arrow-right"></i>
            </a>
          </div>
          {{/if}}
          <a data-type="retrieve" title="{{localize 'BACKPACK_MANAGER.RETRIEVE'}}">
            <i class="fa-solid fa-arrow-right-arrow-left"></i>
          </a>
          <a data-type="delete" title="{{localize 'BACKPACK_MANAGER.DELETE'}}">
            <i class="fa-solid fa-trash"></i>
          </a>
        </div>
      </div>
      {{/each}}
    </div>
  </section>

  {{#unless hideOwnInventory}}
  <section class="inventory">
    <header class="inventory-header" title="{{localize 'BACKPACK_MANAGER.INVENTORY_HEADER'}}">
      <div class="header-name">{{localize 'BACKPACK_MANAGER.INVENTORY'}}: {{actor}}</div>
      <div class="header-encumbrance">({{actorValue}} / {{actorMax}})</div>
    </header>
    <div class="inventory-content">
      {{#each items as |item|}}
      <div class="item">
        <div class="item-details" data-uuid="{{item.item.uuid}}">
          <img src="{{item.item.img}}">
          <a title="{{localize 'BACKPACK_MANAGER.SHOW'}}">
            {{item.item.name}} {{#if (backpackManagerCheckqty item.quantity)}}({{item.quantity}}){{/if}}
          </a>
        </div>
        <div class="functions">
          {{#if (backpackManagerCheckqty item.quantity)}}
          <div class="quantities">
            <a data-type="less" title="{{localize 'BACKPACK_MANAGER.LESS'}}">
              <i class="fa-solid fa-arrow-left"></i>
            </a>
            <input type="number" title="{{localize 'BACKPACK_MANAGER.VALUE'}}" class="current" value="{{item.quantity}}"
              data-max="{{item.quantity}}" disabled>
            <a data-type="more" title="{{localize 'BACKPACK_MANAGER.MORE'}}">
              <i class="fa-solid fa-arrow-right"></i>
            </a>
          </div>
          {{/if}}
          <a data-type="stow" title="{{localize 'BACKPACK_MANAGER.STOW'}}">
            <i class="fa-solid fa-box-archive"></i>
          </a>
        </div>
      </div>
      {{/each}}
    </div>
  </section>
  <footer class="button-section">
    <button name="close">
      <i class="fa-solid fa-briefcase"></i> {{localize "Close"}}
    </button>
  </footer>
  {{/unless}}
</form>
